# Example configuration file. Copy this as `meterpostigo.yml` and fill in the real values
server:
  # Which port the HTTP server should start listening
  port: 9000
database:
    host: localhost
    port: 3306
    username: scott

    # Provide DB password using the environment variable DB_PASSWORD
    password: tiger
    name: readingsdb
cors:
  allowOrigins:
    - http://localhost:3000
